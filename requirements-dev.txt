# Test-only dependencies — NOT needed at runtime, installed in CI on top of
# requirements.txt. pytest is the test RUNNER, not just an import dependency:
# several modules are written pytest-native (bare `def test_*`, pytest.raises,
# the monkeypatch fixture) and Django's runner cannot collect those at all.
# Config lives in pytest.ini. Run the suite with:
#   python -m pytest diet_planner billing analytics login_app
pytest
pytest-django
