Django>=5.1,<6.0
psycopg2-binary>=2.9.0
python-decouple>=3.8
dj-database-url>=2.1.0
gunicorn>=21.2.0
whitenoise>=6.6.0
djangorestframework>=3.14.0
django-encrypted-model-fields>=0.6.5
django-cors-headers>=4.3.1
django-allauth>=65.3.1

# Auth
djangorestframework-simplejwt>=5.3.0
dj-rest-auth[with_social]>=7.0.0

# Admin hardening — TOTP MFA on the Django admin (django-otp) + brute-force
# lockout (django-axes). Protects the public /admin/ surface; user-facing
# allauth/JWT login is untouched. See docs/admin-mfa-hardening.md.
django-otp>=1.5.0
qrcode>=7.4.2
django-axes>=7.0.0

# Async task processing
celery>=5.3.0
redis>=5.0.0

# Validation
pydantic>=2.5.0
pydantic[email]>=2.5.0
email-validator>=2.0.0

# Config
python-dotenv>=1.0.0

# LLM
google-generativeai>=0.3.0

# PDF parsing (LlamaIndex reader only — no embeddings/vector store)
llama-index-core>=0.12.0
llama-index-readers-file>=0.4.0

# HTTP & scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Slack bot + Claude Code integration
slack_sdk>=3.27.0
aiohttp>=3.9.0
claude-agent-sdk>=0.1.0

# Anthropic Claude API — cross-model coherence judge
# (Gemini writes meal plans; Claude grades them). See
# diet_planner/services/recipe_human_judge.py
anthropic>=0.70.0

# Stripe Billing — recurring subscriptions (Standard/Premium tiers).
# See docs/stripe-billing-plan.md and the billing/ app.
stripe>=10.0.0
