# QA Prod Report — 2026-07-16

**VERDICT: GO**

Re-run after the login fix. The seeded QA account now logs in cleanly, and the primary focus of this run — the new **/nastaveni Settings page (PR B)** — passed all 5 sections plus the delete-account rejection path. Public surface and authed navbar/dashboard regression checks also pass. Only minor, non-blocking issues found (see Notes & Czech copy below).

## Results

| Flow | Check | Result | Notes |
|---|---|---|---|
| A — Public | Landing `/` renders (hero, CTA, sections, footer) | PASS | Full Vařto landing renders; CTA "Vytvořit jídelníček zdarma" present; no console errors |
| A — Public | `/recepty` recipe cards | PASS | 24 recipe cards with real detail links (e.g. `/recepty/46/gulasova-polevka-z-mleteho-masa/`); no skeleton stuck; no console errors |
| A — Public | `/pricing` tiers | PASS | 3 tiers render: Zdarma 0 Kč / Standard 99 Kč / Premium 199 Kč + FAQ; no console errors |
| B — Auth | Login via /login (seeded QA account) | PASS | Redirects to /onboarding, navbar shows account email. One 401 on `/api/analytics/consent/` fired during login (consent-sync race) — non-blocking, see Console errors |
| B — Navbar | Account dropdown: email + "Nastavení" link | PASS | Dropdown shows `qa+52a395@eatalnicek.eu`, "Nastavení" → /nastaveni, "Odhlásit se" present |
| B — Settings §1 | Předvolby form renders (goal/style/allergies/household/budget/skill/time/country/num_days) | PASS | All 9 controls present |
| B — Settings §1 | Save + persist + merge | PASS | Set goal="Chci jíst zdravěji" + num_days 7→5, toast **"Předvolby uloženy"**; after reload both persisted AND pre-existing household=2, country=CZ, budget=1500 untouched — backend merge confirmed |
| B — Settings §2 | Účet: login method + badge | PASS | Shows "Přihlašovací metoda: E-mail" + "Neověřeno" badge (correct for the seeded account) |
| B — Settings §2 | Změnit heslo client validation (no real change) | PASS | Too-short → "Nové heslo musí mít alespoň 8 znaků."; mismatch → "Nová hesla se neshodují." Password never actually changed |
| B — Settings §3 | Předplatné: tier + remaining + link | PASS | "Aktuální tarif: Zdarma", "Zbývá 2 generování zdarma", "Zobrazit tarify" routes to /pricing |
| B — Settings §4 | Soukromí: marketing-cookies toggle | PASS | Povoleno→Zakázáno with toast "Souhlas uložen", state persisted across reload, flipped back to Povoleno (restored) |
| B — Settings §5 | Moje data: JSON export | PASS | `moje-data.json` downloaded; valid JSON with keys `account, preferences, usage, subscription, marketing_consent, meal_plans` |
| B — Delete | Modal danger copy | PASS | "Tato akce je nevratná…" incl. subscription-forfeit warning |
| B — Delete | Confirm disabled until password entered | PASS | "Trvale smazat" is `disabled` with empty password field |
| B — Delete | Wrong password rejected | PASS | Inline error **"Nesprávné heslo."**; API returned 403 on `/api/auth/account/`; still logged in; account intact (deletion never completed) |
| B — Delete | Zrušit closes modal | PASS | Modal gone, session alive |
| B — Regression | Dashboard/plan list loads post-PR-B navbar | PASS | Account has 0 plans → "/" cleanly redirects to /onboarding (expected empty-state); navbar links (Moje plány / Nový plán / Ceník) all render |

## Console errors

- `401` on `https://eatalnicek.eu/api/analytics/consent/` — fired once immediately during login (consent-sync request raced session establishment). Cosmetic; consent later saved fine from /nastaveni. Worth a follow-up but not blocking.
- `403` on `https://eatalnicek.eu/api/auth/account/` — **intentional**: our wrong-password delete-rejection test. Correct behavior, not a bug.

No other console errors on any page visited (landing, /recepty, /pricing, /login, /nastaveni, dashboard).

## Notes / minor issues (non-blocking)

1. **SPA page title not updated on /nastaveni** — `<title>` stays "Přihlášení — Vařto" (after login navigation) or the landing title (after direct load) instead of e.g. "Nastavení — Vařto".
2. **Czech copy:** authed plan-list loading state shows **"Nacitani..."** (missing diacritics — should be "Načítání…") under a **"Generujeme..."** heading, which misleadingly implies plan generation when it's just loading the plan list.
3. **Czech copy:** dietary-style chip **"Vysoko proteinové"** should be one word: **"Vysokoproteinové"** (matches landing copy "vysokoproteinová").
4. Delete-modal password field triggers a Chrome verbose hint (password field not in a `<form>`) — a11y/autofill nicety only.

## Screenshots

No failures — no failure screenshots taken. (Authed-run artifacts stay in local `.playwright-mcp/`, not committed; the downloaded `moje-data.json` is also there.)

## Skipped

- Plan generation + Stripe checkout reach: not in scope for this run (task focus was the PR B Settings page + light regression; account's 2 free generations preserved).
- Recipe detail / legal pages / mobile viewport: not in scope for this run's lighter public sanity check.
