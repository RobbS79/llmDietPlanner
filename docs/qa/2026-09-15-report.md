# QA Prod Report — 2026-09-15

**VERDICT: GO**

Target: https://eatalnicek.eu (live, Stripe LIVE keys). Run type: post-DATA-change verification (curated corpus 442 → 505 published, batches 07+08) and first real-plan check of demand-driven ranking (live since 2026-09-10, previously unverifiable due to Gemini outage). No code deploy today.

Authed flow ran as seeded QA account `qa_bot_17b32e` (user 24) via token injection into localStorage. Exactly ONE plan was generated (plan #148, 5 days, prompt "klasická česká kuchyně – guláš, svíčková, řízek, knedlíky", Praha). Exactly ONE refine-chat swap was accepted. Stripe Checkout was reached and abandoned; no card data entered. No data deleted.

Harness note: the Playwright MCP browser could not launch in this environment (Chromium refuses to run as root without `--no-sandbox`, two attempts). The run was executed instead with the repo's own `e2e/node_modules/playwright` Chromium (`--no-sandbox`, 1280x900, locale cs-CZ) through a small driver process, with console `error`/`warning`/`pageerror` and HTTP >= 400 responses captured on every page. Screenshots therefore live in the session scratchpad (paths below), not `.playwright-mcp/`.

## Results

| Flow | Check | Result | Notes |
|---|---|---|---|
| A | `/` landing renders (200, hero, CTA) | PASS | 200; title "Vařto — Jídelníček na míru podle vašeho cíle"; H1 "Zhubnout, nabrat, nebo jen jíst líp? Naplánujeme vám celý týden jídla."; primary CTA "Vytvořit jídelníček zdarma" routes to `/login` (login form with username/password + Google present). 0 console errors. |
| A | Consent banner Přijmout / Odmítnout | PASS | Banner shows both buttons. "Odmítnout" dismisses (0 banner buttons after click). After clearing storage + reload the banner returns; "Přijmout" dismisses and the choice persists across reload. |
| A | `/recepty` 200, cards render, links resolve | PASS | 200; H1 "Prozkoumejte recepty."; 24 recipe cards, 0 skeleton nodes stuck; every card is a real `<a href="/recepty/<id>/<slug>/">`. Also verified at 390x844 (24 cards). |
| A | 3 recipe detail links -> 200, ingredients amount+name, no `(#id)` | PASS | `/recepty/75/has-ze-sladkych-brambor/` (538 kcal), `/recepty/67/domaci-bramborove-halusky/` (308 kcal), `/recepty/63/spenat-s-bramborovym-knedlikem/` (262 kcal): all 200, ingredient lines like `300 g / brambory`, `0,5 ks / vejce`, `2 lžičky / řepkový olej`; regex `\(#\d+\)` found 0 matches on all three; numbered Postup present. |
| A | `/pricing` renders 3 tiers | PASS | 200; H1 "Jednoduchý ceník."; tiers Zdarma (0 Kč), Standard (99 Kč/měsíc), Premium (199 Kč/měsíc); CTAs "Začít zdarma / Vybrat Standard / Vybrat Premium". |
| A | Legal page from footer | PASS | Footer links `/privacy` + `/terms`; `/privacy` 200, H1 "Zásady ochrany soukromí.", real numbered sections (1. Správce údajů, 2. Jaké údaje shromažďujeme, ...), 1.5 kB of copy. |
| A | Mobile 390x844 hamburger opens + navigates | PASS | Header button `aria-label="Otevřít menu"` visible; after click the menu exposes Recepty / Ceník / Přihlásit se / Vytvořit jídelníček / Jak to funguje / O nás / privacy / terms; tapping Recepty navigates to `/recepty` and cards render. |
| B | Token injection -> authed | PASS | After setting `access_token`/`refresh_token` and reloading, header shows the QA account e-mail, "Moje plány / Nový plán", dashboard "3 plánů zdarma zbývá". No 401 seen on `/api/auth/profile/` in this run. |
| B1 | Plan generation succeeds, no hollow/stub days | PASS | Plan #148 reached "Plán připraven" in ~40 s. 5 days x 3 slots = 15/15 meals filled, each with name, description, macros, kcal, prep time. Avg 1831 kcal/day, 118 g protein. No error state. Screenshot: `plan-148-overview.png`. |
| B2 | Demand ranking: mains are Czech classics, not salads/bowls | PASS | Lunch/dinner mains (10): Rajská omáčka s masovými kuličkami; Domácí rizoto; Expresní svíčková na smetaně; Segedínský guláš; Masové karbanátky; Lečo; Přírodní vepřový řízek; Fazolový guláš s klobásou; Kuře pečené v máslové marinádě; Domácí kulajda. 10/10 recognisable Czech dishes, 0 salads, 0 bowls, 0 Anglo/US. Prompt terms guláš / svíčková / řízek / knedlíky all honoured. No dish repeats verbatim. (Breakfasts: Palačinky, Sýrová omeleta se šunkou, Tvarohové palačinky, Míchaná vejce, Ovesná kaše.) |
| B3 | Lečo rule (max once, dinner only, with bread) | PASS | Lečo appears exactly once: Day 3 VEČEŘE, side line "s chlebem · 2 krajíce". |
| B4 | Příloha on 2 mains + side ingredient in the recipe's shopping list | PASS | Segedínský guláš (day 2 dinner): overview line "s houskovým knedlíkem · 3 plátky"; recipe page has a separate **PŘÍLOHA** group containing `120 g houskový knedlík`. Přírodní vepřový řízek (day 4 lunch): overview "s vařenými bramborami · 250 g"; recipe **PŘÍLOHA** group `250 g vařené brambory`. The per-recipe ingredient list (Ingredience + Příloha + Volitelné) is the app's shopping unit; there is no separate shopping-list control (whole-plan list was removed by design), and the side is present in that list with an amount. Overview sides across plan: knedlík x3, vařené brambory x3, chléb x2; Domácí rizoto and Domácí kulajda (complete dishes) have none. |
| B5 | Recipes render: amount-unit-name, no `(#id)`, instructions, plausible kcal | PASS | None of the newly added batch-07/08 recipes surfaced in this plan, so two existing mains were opened (+ the swapped-in one). Segedínský guláš: 14 ingredients (`125 g vepřové maso (plec, kýta nebo krkovice)`, `125 g kysané zelí`, `38 ml smetana na vaření 12%`, ...), 6 Postup steps, 498 kcal/porce, attribution "Inspirováno receptem z Toprecepty.cz", 0 `(#id)`. Přírodní vepřový řízek: 8 ingredients, 6 steps, 461 kcal, 0 `(#id)`. Tradiční kuře na paprice (after swap): 9 ingredients, 792 kcal, 0 `(#id)`. Plan-level kcal range for mains 461–928, soups/breakfasts 370–605 — all within plausibility bounds. Screenshots: `recipe-segedinsky-gulas.png`, `recipe-veprovy-rizek.png`. |
| B6 | Refine chat: one swap, accepted, meal changed, no console errors | PASS | On the řízek page "Poradit se s kuchařkou" opened an inline chat. Sent "vyměň tohle za něco jiného českého". Assistant narrated a web search (stages "Chystám hledání… / Hledám recepty na webu / Čtu nalezený recept") and after ~88 s offered "Tradiční kuře na paprice (podle Apetit Online)" 114 min / 792 kcal with "Použít tento recept". Accepted -> recipe page H1 became "TRADIČNÍ KUŘE NA PAPRICE." within 4 s, with PŘÍLOHA `120 g houskový knedlík`; plan overview Day 4 OBĚD now shows "Tradiční kuře na paprice · s houskovým knedlíkem · 3 plátky · 792 kcal". 0 console errors. Screenshots: `chat-candidates.png`, `after-swap-recipe.png`, `plan-148-after-swap.png`. |
| B | Checkout reaches Stripe | PASS | Authed, `/pricing` -> "Vybrat Standard" -> redirected to `https://checkout.stripe.com/c/pay/cs_live_…` showing "Varto Standard, fakturováno měsíčně". Stopped immediately; nothing entered. Screenshot: `stripe-checkout.png`. |
| C | Console errors across all pages | PASS | 0 `error`/`pageerror` entries and 0 HTTP >= 400 responses on any eatalnicek.eu page visited (landing, login, recepty, 3 recipe details, pricing, privacy, mobile landing/recepty, dashboard, create, plan/148, 3 plan recipe pages, chat). Warnings only — listed below. |

## Console errors

none.

Warnings observed (not errors):
- `/pricing` (authed, on checkout click): `[Meta Pixel] - You are attempting to send a restricted event. The event was suppressed. Go to Events Manager to learn more.`
- `checkout.stripe.com` (third-party page): `<link rel=preload> uses an unsupported 'as' value` (x2) — Stripe's own page, not ours.

## Screenshots

Directory (session scratchpad, not committed; authed shots contain the QA account e-mail):
`/tmp/claude-0/-opt-llmDietPlanner/2c68dcc9-4ced-43eb-9768-60ebef36cd65/scratchpad/shots/`

- `landing.png`, `recepty.png`, `recipe-63.png`, `pricing.png`, `mobile-before.png`, `mobile-menu-open.png`, `mobile-recepty.png`
- `create-form.png`, `create-step2.png`
- `plan-148-overview.png` (plan overview, requested), `recipe-segedinsky-gulas.png` (recipe page, requested), `recipe-veprovy-rizek.png`
- `chat-open.png`, `chat-candidates.png`, `after-swap-recipe.png`, `plan-148-after-swap.png`
- `stripe-checkout.png`

No FAIL screenshots (no failures).

## Notes (cosmetic / follow-ups, not verdict-affecting)

1. **Demand ranking observation.** First real plan with the new ranker looks right for the Czech-classics prompt: all 10 mains are Czech, two are prompt terms verbatim (svíčková, řízek), guláš appears as two distinct variants (Segedínský at day 2 dinner, Fazolový s klobásou at day 4 dinner) — not a repeat, but same dish family twice in 5 days is worth a glance at the dedupe rules. Domácí kulajda (a soup) is served as the sole dinner main on day 5 at 600 kcal — acceptable, but a soup-as-dinner without bread/side may read as thin to some users.
2. **New batch recipes did not surface.** None of the named batch-07/08 dishes (kapustnice, kuřecí paprikáš, koleno, carbonara, gyros, pho, šopský, králík, hrachová kaše, boloňské) appeared in this plan; the chat swap instead did a live web search for kuře na paprice ("podle Apetit Online") rather than pulling the corpus "Kuřecí paprikáš". Not a failure, but if the intent was for new corpus items to be reachable from the chat, that path did not exercise them.
3. **Stripe Checkout presentation.** The live Checkout page rendered in EUR ("4,24 € za měsíc", "1 CZK = 0,0428 EUR (zahrnuje konverzní poplatek 4 %)") for a cs-CZ browser, presumably Stripe adaptive pricing keyed on the server's IP geolocation. A Czech card-holder may see 99 Kč on /pricing and a EUR figure with a 4 % fee on Checkout. Merchant label shows "mealPlanner" (Back link) and product "Varto" without háček — same branding items flagged in the 2026-07-13 preflight audit.
4. **Chat register.** Assistant opener "Ráda bys zkusila jinou českou klasiku? Už pro tebe…" addresses the user as female singular / tykání, then "Mrkněte na návrh níže" switches to vykání. Cosmetic copy inconsistency.
5. **Accessibility.** Meal cards on the plan overview are `div.cursor-pointer` wrapping an `<h3>`; "Zobrazit recept" is a hover-only label, not a link/button. Keyboard users cannot open a recipe from the overview. The e2e suite would not catch this.
6. **Dashboard residue.** The QA account's plan list still shows plan #147 (2026-09-13) in FAILED state from the Gemini outage window — visible to the QA user only, but confirms the failure state renders.
7. **Harness.** Playwright MCP is broken in this environment (root + Chromium sandbox). The fallback driver used here is `scratchpad/driver.mjs`; if the MCP is expected to work, either run the agent as non-root or launch the MCP with `--no-sandbox`.

**VERDICT: GO**
