# QA Prod Report — 2026-09-22

**VERDICT: GO**

- Base URL: https://eatalnicek.eu
- Deploy under test: prod commit `f09e162` = PR #101 "feat(billing): promo codes (100% no-card grants + Stripe coupon path)"
- Scope: public-only verification of the promo-code UI on /pricing (baseline, invalid-code path, deep link, unauthenticated login hand-off, pending-promo banner), public regression smoke (/, /recepty, recipe detail, /jak-to-funguje), mobile 390x844.
- Flow B (authed: login, plan generation, Stripe checkout): **SKIPPED** — no QA credentials available this session (prod exec harness that mints the QA token is blocked). No account was registered, no login attempted, nothing entered on Stripe.
- Promo data: no promo code exists in the prod DB yet, so only the no-code and invalid-code (`not_found`) paths were exercised. No real codes were guessed.
- Harness note: the Playwright MCP server could not launch Chrome (running as root without `--no-sandbox`, MCP config has no `--no-sandbox`). Checks were run through a real headless Chromium 1223 via the repo's `e2e/node_modules/playwright` 1.60 with `chromiumSandbox: false`; driver script at `/tmp/claude-0/-opt-llmDietPlanner/28572e61-359b-41c2-a177-343fd50f85a8/scratchpad/qa.js`, raw results in `results.json` next to it. Console messages, page errors and failed requests were captured per page.

## Results

| Section | Check | Result | Evidence |
|---|---|---|---|
| A | 1. 3 plan cards Zdarma 0 Kč / Standard 99 Kč/měsíc / Premium 199 Kč/měsíc | PASS | All three names and prices present; "Kč/měsíc" on Standard and Premium; desktop screenshot `2026-09-22-01-pricing-desktop-after-promo.png` |
| A | 1. "Doporučeno" badge on Standard | PASS | Badge found inside the Standard card (ancestor contains "Standard"+"99", not "Premium") |
| A | 1. FAQ 5 questions, accordion opens/closes | PASS | 5 question buttons; first "Kolik jídelníčků mohu zdarma vygenerovat?"; click expands body text, second click collapses |
| A | 1. Footer privacy/terms links | PASS | `Zásady ochrany soukromí → /privacy`, `Obchodní podmínky → /terms` |
| A | 1. Header hamburger on mobile | PASS | Header button `aria-label="Otevřít menu"`; click shows nav (link count 6 → 12) |
| A | 2. "Mám promo kód" with tag icon, under grid, above "Stojí méně…" | PASS | Button contains `svg.lucide-tag`, 12 px text; y=106 vs cards bottom −467 vs kafe paragraph y=192 (button between cards and paragraph) |
| A | 3. Click reveals form: uppercase input, placeholder "Promo kód", green "Použít" disabled when empty | PASS | input `text-transform: uppercase`, `autocapitalize=characters`, value ""; button "Použít" `disabled=true`, bg `rgb(46,107,67)`, white text |
| A | 4. NOPE123 → red centered "Tento kód neznáme."; prices unchanged | PASS | Button enabled after typing; Enter submits; message color `rgb(178,62,28)`, `text-align: center`; 99/199 still present, 0 struck-through elements, no "bez platební karty", CTAs "Vybrat Standard"/"Vybrat Premium" intact, no "Aktivovat zdarma" |
| A | 5. Network: validate request | PASS | `GET https://eatalnicek.eu/api/billing/promo/validate/?code=NOPE123` → **200**, body `{"valid":false,"reason":"not_found"}` |
| A | 6. Lowercase "abc" shown uppercase (CSS), request sent, not_found | PASS | DOM value "abc", rendered uppercase via CSS (visible as "ABC" in screenshot); request `?code=ABC` → 200 not_found (frontend uppercases before sending); message shown |
| B | 7. `/pricing?promo=NOPE123` deep link | PASS | Box auto-opened, input pre-filled "NOPE123", "Tento kód neznáme." shown without a click, validate request 200 not_found, URL became `https://eatalnicek.eu/pricing` (param stripped), `pending_promo` = null |
| C | 8. "Vybrat Standard" (unauth) → `/login?next=/pricing` renders | PASS | URL `https://eatalnicek.eu/login?next=/pricing`, HTTP 200; tabs PŘIHLÁŠENÍ / REGISTRACE, username + password inputs, "PŘIHLÁSIT SE", "POKRAČOVAT PŘES GOOGLE"; no page error. (My driver first flagged this because the tab text is CSS-uppercased and my regex was case-sensitive; re-inspected page text confirms it renders — screenshot `2026-09-22-06-login-next-pricing.png`) |
| C | 9. No green banner on /login; `localStorage.pending_promo` null | PASS | banner count 0; `localStorage.getItem('pending_promo')` === null (not_found is terminal, nothing stored) |
| C | 10a. Banner for `pending_promo=TESTBANNER`; "zrušit" clears | PASS | After reload: "Kód **TESTBANNER** se uplatní po přihlášení." in green box (bg `rgb(231,240,232)`, border `rgba(46,107,67,.3)`), "zrušit" button present; after click banner gone and key removed (null). Screenshot `2026-09-22-03-login-promo-banner.png` |
| C | 10b. /pricing with `pending_promo=TESTBANNER` auto-validates and clears key | PASS | Box auto-open, input "TESTBANNER", "Tento kód neznáme." shown, `GET …/validate/?code=TESTBANNER` → 200 not_found, `pending_promo` null afterwards |
| C | 10c. Cleanup | PASS | `pending_promo` null at end (fresh browser context, nothing persisted) |
| D | 11. Landing `/` loads, no console errors | PASS | 200; h1 "Zhubnout, nabrat, nebo jen jíst líp? Naplánujeme vám celý týden jídla."; consent banner present; 0 console messages |
| D | 12. /recepty cards + one recipe page | PASS | /recepty 200, 24 recipe links, 0 skeletons; `/recepty/82/venkovske-snidanove-misky/` 200, h1 "Venkovské snídaňové misky.", Suroviny + Postup sections present |
| D | 13. /jak-to-funguje loads | PASS | 200; h1 "Jak napsat zadání, ze kterého vznikne dobrý jídelníček."; 5.7k chars body |
| E | 14. Mobile 390x844 /pricing, promo box open | PASS | `scrollWidth=390 <= innerWidth=390` (no horizontal scroll); input x 24–271, button x 279–366 (both inside viewport, side by side); cards stacked vertically (tops 526 / 1131 / 1695, left 57/58/57 px). Screenshot `2026-09-22-04-pricing-mobile-promo-open.png`. (Driver flagged only because of a 1 px left-offset tolerance — visual check confirms the stack) |
| B (authed) | Login / plan generation / Stripe checkout | SKIP | Flow B disabled — no QA credentials this session |

## Console errors

None. Zero console messages (any level), zero `pageerror`, zero failed requests on every page visited: `/pricing`, `/pricing?promo=NOPE123`, `/login?next=/pricing`, `/`, `/recepty`, `/recepty/82/…`, `/jak-to-funguje`, `/pricing` (mobile). Note the consent banner was never accepted, so the Meta Pixel was not loaded and the known "restricted event" warning could not appear.

## Screenshots (local `.playwright-mcp/`, not committed)

No genuine failures. Evidence screenshots:

- `/opt/llmDietPlanner/.playwright-mcp/2026-09-22-01-pricing-desktop-after-promo.png` — desktop /pricing after invalid code "ABC" (red message, prices unchanged)
- `/opt/llmDietPlanner/.playwright-mcp/2026-09-22-03-login-promo-banner.png` — /login green banner for TESTBANNER
- `/opt/llmDietPlanner/.playwright-mcp/2026-09-22-04-pricing-mobile-promo-open.png` — mobile /pricing with promo box open
- `/opt/llmDietPlanner/.playwright-mcp/2026-09-22-06-login-next-pricing.png` — /login?next=/pricing render
- `2026-09-22-02-…` and `2026-09-22-05-…` — auto-captured on the two driver false positives (C8, E14), kept for reference

## Not covered / follow-ups

- Valid-code paths (struck-through price, "bez platební karty", "Aktivovat zdarma", redeem POST, Stripe coupon checkout) are untested: no promo code exists in prod yet. Re-run once the owner creates one (and ideally with Flow B creds).
- Playwright MCP is currently unusable on this box (root + no `--no-sandbox`); add `--no-sandbox` to the `playwright` entry in `/opt/llmDietPlanner/.mcp.json` args, or run non-root, before the next `/qa-prod`.

**VERDICT: GO** — no failing rows.

---

## Addendum (same night) — authed 100 % grant path: **GO**

Run after the owner re-authed doctl. Prod deployment `49da0668` ACTIVE (same
code `f09e162`; the redeploy only removed a stray env var). Test code
`VARTO100` (100 %, lifetime, Premium only, max 20) created on prod via the
exec harness; QA bot user 24 logged in by minted JWT (localStorage
`access_token`/`refresh_token`), headless Chromium 1223 via `e2e/` Playwright.

| # | Check | Result | Evidence |
|---|---|---|---|
| 1 | `/pricing?promo=VARTO100` authed → validate 200 `valid:true`, prices `{premium: 199→0}` | PASS | `GET /api/billing/promo/validate/?code=VARTO100` → 200 |
| 2 | Premium card: struck **199 Kč**, "0 Kč/měsíc", green "bez platební karty"; Standard card untouched (code is Premium-only) | PASS | `2026-09-22-grant-01-pricing-applied.png`; 1 struck element; "Vybrat Standard" still present |
| 3 | CTA text "Aktivovat zdarma" only on Premium; message "Kód VARTO100: sleva 100 % navždy." | PASS | 1 CTA |
| 4 | `?promo=` param stripped from URL after apply | PASS | URL `/pricing` |
| 5 | Click "Aktivovat zdarma" → `POST /api/billing/promo/redeem/` → 200 `{"granted":true,"tier":"premium"}`, no Stripe | PASS | network log |
| 6 | Lands on `/` (param `promo=granted` consumed) with toast "Váš tarif je aktivní přes promo kód. Dobrou chuť!" | PASS | `2026-09-22-grant-02-dashboard-toast.png` |
| 7 | `/api/billing/me/` → tier premium, status active, **source promo**, entitled, `remaining_quota` 30, `grant_expires_at` null | PASS | JSON |
| 8 | `/nastaveni` Předplatné: "Premium **přes promo kód**", "Platí navždy", "Využito 0 z 30 tento měsíc", **no** "Spravovat předplatné" (Stripe portal) button | PASS | `2026-09-22-grant-05-settings-subscription.png` |
| 9 | Second `/pricing?promo=VARTO100` + activate → redeem 400 `already_redeemed`, UI "Tento kód jste už použili." | PASS | network log; the only console "error" is the browser logging this expected 400 |
| 10 | Cleanup: QA bot's promo `Subscription` + `PromoRedemption` deleted on prod → `/billing/me` back to `subscription: null`, VARTO100 redemptions 0 of 20 | PASS | harness output |

Not covered: the percent-code (Stripe coupon) checkout path — no 1–99 % code
exists; needs a Stripe test-mode coupon before a real campaign.

**Finding (pre-existing, not promo-specific):** the dashboard header always
prints `{free_generations_remaining} plánů zdarma zbývá` (`pages/Dashboard.tsx`),
so a freshly granted Premium user sees the success toast next to "2 plánů
zdarma zbývá". Same for Stripe subscribers. Suggest reading `/billing/me` there
and showing the tier + monthly quota when `entitled`.
