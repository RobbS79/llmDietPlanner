# QA Prod Report — 2026-08-05 (run 2, post-deploy of PR #54 / commit 1bf6204)

**VERDICT: NO-GO**

Reason: the user-visible damage the deploy was meant to address is still being served in prod — the public recipe page `/recepty/62/dynove-muffiny-bez-lepku/` shows an obvious batch-fraction artifact (recipe labeled "1 porce" with 1/12-batch ingredients — 14 g flour, 0,5 vejce — while the instructions describe baking a 12-muffin batch) and has **no nutrition section at all**; and the QA account's plan #134 still renders the broken 684 kcal/day plan with a 16-kcal breakfast. The new code itself could not be exercised (fresh generation unreachable: Flow B disabled and the QA account has 0 free plans left), so the deploy is unverified AND broken artifacts remain public.

## Results

| Flow | Check | Result | Notes |
|---|---|---|---|
| A | Landing `/` — hero + primary CTA | PASS | Hero "Zhubnout, nabrat, nebo jen jíst líp?…" renders; CTA "Vytvořit jídelníček zdarma" routes to `/login` (correct for logged-out user); footer + sample-plan mock render. |
| A | Landing `/` — consent banner | PASS | Banner appears with cleared storage. "Odmítnout" dismisses and persists `{"consent":false}`; "Přijmout" dismisses, persists `{"consent":true}` and loads Meta Pixel (`window.fbq` = function). |
| A | `/recepty` — recipe grid | PASS (with notes) | 24 real cards on page 1 (of 2), images + descriptions + discount badges, real links to details, pagination present. **Note (minor):** several cards carry mismatched slugs — "Menemen" → `/recepty/56/thajsky-okurkovy-salat/`, "Gulášová polévka z mletého masa" → `/recepty/58/italsky-sekany-salat/`, "Řecká cizrnová polévka" → `/recepty/54/italsky-sekany-salat/`. ID wins so the correct recipe renders, but slugs are wrong (SEO/dup-content risk, suggests slug not regenerated when Recipe rows are reused). |
| A | Recipe detail `/recepty/63/spenat-s-bramborovym-knedlikem/` | PASS (with notes) | Full content: ingredients, 8 steps, attribution (Toprecepty.cz), discounts, nutrition 262 kcal / 23 g B / 14 g S / 11 g T per portion — plausible. **Note (minor):** ingredient list is per-portion scaled (0,5 ks vejce, 7 g másla) while step text uses full-batch amounts (1 vejce, 20 g másla) — confusing but readable. |
| A | Recipe detail `/recepty/56/…` (Menemen, mismatched slug) | PASS | Renders correct recipe by ID; coherent content; nutrition 659 kcal/portion (high-ish for 1 egg + veg, but within meal-plausible range). |
| A | **Recipe detail `/recepty/62/dynove-muffiny-bez-lepku/`** | **FAIL** | **Batch-fraction artifact live on the PUBLIC surface**: recipe says "1 porce" but ingredients are the 1/12 batch fraction (14 g mandlová mouka, 0,5 ks vejce, 20 ml dýňového pyré, 7 ml sirupu) while Postup step 1 says "připravte si formu na 12 muffinů" and step 5 "zbývající těsto rovnoměrně rozdělte". The "Nutriční hodnoty" section is entirely absent from the page. This is the exact recipe behind the 16-kcal-breakfast incident; the broken pre-fix data leaked into the public Recipe showcase and is still served. Screenshot: `/opt/llmDietPlanner/.playwright-mcp/fail-recipe-62-muffiny-batch-fraction.png` |
| A | `/pricing` | PASS | Three tiers render: Zdarma 0 Kč / Standard 99 Kč/měsíc (Doporučeno) / Premium 199 Kč/měsíc, feature lists + FAQ + CTA. |
| A | Legal `/privacy` (footer link) | PASS | Real content, 6 sections (Správce údajů … Zabezpečení), cookies section present. |
| A | Mobile 390×844 — hamburger nav | PASS | "Otevřít menu" opens (Recepty / Ceník / Přihlásit se / Vytvořit jídelníček); tapping "Recepty" navigates correctly. |
| A | No raw Python errors / tracebacks / 500s | PASS | None observed on any tested page; no console errors anywhere. |
| A* | Plan #134 calorie plausibility (read-only view via pre-existing QA session) | FAIL (pre-fix data, informational) | Browser had a leftover session for the seeded QA account (`qa+52a395@eatalnicek.eu`); viewed completed plan #134 read-only. It still shows **Prům. kcal/den 684** with Den 1 snídaně "Dýňové muffiny bez lepku" at **16 kcal** and multiple meals under 150 kcal (90, 105, 165 kcal). This is data generated before the fix (fix is not retroactive), but it remains user-visible. Logged out afterwards; no data mutated. |
| B | Login with QA credentials | SKIP | Flow B disabled — QA_TEST_USERNAME/QA_TEST_PASSWORD not available in this environment. |
| B | Generate fresh plan (verify slot-default calories 450/650/550/250) | SKIP | Flow B disabled. Additionally, the QA account shows "0 plánů zdarma zbývá", so even an authed run could not generate without an entitlement top-up. **The core fix of commit 1bf6204 is therefore unverified end-to-end.** |
| B | Reach Stripe Checkout | SKIP | Flow B disabled. |

## Coverage gaps

- **Fresh plan generation was unreachable**: no public flow generates or previews a real plan (landing sample plan is static marketing markup), Flow B was disabled, and the seeded QA account has 0 free plans remaining. The per-slot calorie defaults and nutritional_info-shape tolerance introduced in commit 1bf6204 could not be exercised. To close this: enable Flow B with credentials and top up the QA account's plan quota, then generate and check per-meal kcal (expect breakfast ≈450 / lunch ≈650 / dinner ≈550 defaults when Gemini omits usable nutrition).
- Authed checkout → Stripe not tested (Flow B disabled).

## Console errors

None. Zero console errors/warnings on: landing (desktop + mobile), /recepty, recipe details 63/56/62, /pricing, /privacy, /login, plan #134 view.

## Screenshots (local, gitignored — do not commit; may contain session data)

- `/opt/llmDietPlanner/.playwright-mcp/fail-recipe-62-muffiny-batch-fraction.png` — public muffin recipe with 1/12-batch ingredients and missing nutrition section.
- Accessibility snapshots for all visited pages in `/opt/llmDietPlanner/.playwright-mcp/page-2026-08-05T23-3*.yml` (includes the plan #134 view showing 684 kcal/day and the 16-kcal breakfast).

## Recommended follow-ups

1. Data repair: regenerate or unpublish public Recipe 62 (and audit Recipes 61–63 created around plan #134) — rescale ingredients to true per-portion or per-batch, restore nutrition block.
2. Repair or regenerate plan #134 for the QA account (or accept as historical, but note any real user with a pre-fix plan sees the same).
3. Fix slug generation for reused Recipe rows (ids 54/56/58 carry slugs of previous recipe content).
4. Re-run this QA with Flow B enabled + plan quota so the actual fix can be verified with a fresh generation.

**VERDICT: NO-GO** — deploy's target defect still user-visible in prod (public recipe 62 batch-fraction artifact + missing nutrition; plan #134 still 684 kcal/day) and the fix itself is unverified because fresh generation was unreachable.
