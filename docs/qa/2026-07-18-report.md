# QA Prod Report — 2026-07-18

**VERDICT: GO**

Deploy under test: commit `ddeede6` — "feat(refine-chat): past suggestions stay clickable to re-expand (#42)".
Deployed bundle confirmed: `/static/assets/index-CECUM5Dv.js` → HTTP 200 (451,894 bytes), and it is the only asset script referenced by the served page. This pins the run to the new deploy.

**Scope note:** The changed feature (refine-chat suggestion history, authed RecipePage panel) could NOT be exercised — Flow B is disabled in this environment (QA credentials are DO-side secrets). That feature is covered by component tests + CI only. This run is a public-surface regression pass, which is the correct exposure check for a pure-frontend authed-panel change: it verifies the new bundle didn't break anything public.

## Results

| Flow | Check | Result | Notes |
|---|---|---|---|
| A | Landing `/` renders (hero, headline, sample plan, footer) | PASS | Vařto branding + Market Paper light theme intact; SSR title "Vařto — Jídelníček na míru podle vašeho cíle" and meta description intact |
| A | Primary CTA routes correctly | PASS | "Vytvořit jídelníček zdarma" → `/login` with full login form |
| A | Consent banner — "Odmítnout" | PASS | Banner dismisses, `mkt_consent_v1` persisted |
| A | Consent banner — "Přijmout" | PASS | Banner dismisses, consent stored `{consent:true}`, Meta Pixel (`fbq`) loads only after accept |
| A | Landing console errors (clean session) | PASS | 0 errors |
| A | `/recepty` cards render | PASS | 24 cards, 24 images all loaded (0 broken), 0 stuck skeletons, cards link to detail pages |
| A | Recipe detail (`/recepty/53/kureci-parmigiana/`) | PASS | H1, Ingredience, Postup, Nutriční hodnoty all present; deals headline "4 z 10 surovin ve slevě" renders; SSR title "Kuřecí parmigiana — Recept \| Vařto" |
| A | Login page + form | PASS | Username/password fields, submit, Google SSO, forgot-password link |
| A | Signup ("Registrace") tab + form | PASS | Username/email/password/confirm + "Vytvořit účet" present (no account created) |
| A | `/pricing` tiers | PASS | Three tiers: Zdarma 0 Kč / Standard 99 Kč / Premium 199 Kč; FAQ + CTA present |
| A | Legal pages `/privacy`, `/terms`, footer `/o-nas` | PASS | Real content on all three; privacy has 6 sections incl. §4 Cookies mentioning Pixel |
| A | Mobile 390×844 — hamburger header | PASS | "Otevřít menu" opens (toggles to "Zavřít menu", `aria-expanded`), menu links navigate to `/recepty` |
| A | Console errors across all pages | PASS | 0 errors on every page in a clean session (see below for stale-session note) |
| B | Login as QA account | SKIP | Flow B disabled — QA_TEST_USERNAME/QA_TEST_PASSWORD not available (DO-side secrets) |
| B | Onboarding + plan generation | SKIP | Flow B disabled |
| B | Refine-chat suggestion history (the deployed change) | SKIP | Authed-only; verified by component tests + CI, not exercisable here |
| B | Reach Stripe Checkout | SKIP | Flow B disabled |

## Console errors

None in a clean session (all pages: landing, /recepty, recipe detail, /login incl. Registrace tab, /pricing, /privacy, /terms, /o-nas, mobile landing + nav).

One-time errors observed only with **stale auth state left over from a previous browser session** (before storage was cleared):

- `GET /api/auth/profile/` → 401
- `POST /api/auth/refresh/` → 500

The app degraded gracefully (redirected to `/login`). Direct probes of the refresh endpoint return correct codes (invalid token → 401, empty body → 400); the 500 appears specific to a stale-cookie state, is backend-side, and is unrelated to this pure-frontend deploy. Filed here as a non-blocking observation worth a backend look.

## Other non-blocking observations (pre-existing, not regressions)

- `/recepty` index serves the generic landing `<title>`/meta in SSR — the route is absent from `frontend/prerender.mjs`'s route list (recipe *detail* pages do get proper titles from Django SSR). Minor SEO polish gap.
- `/recepty` card data quirks: duplicate "Kuřecí parmigiana" (ids 52 & 53); slug/title mismatches (id 50 slug `ovesna-kase` titled "Západoafrická arašídová polévka", id 49 slug `chana-masala` titled "Gulášová polévka z mletého masa"). Content data, not rendering.

## Screenshots

No failures — no failure screenshots taken. Run artifacts (page snapshots) are in the local `.playwright-mcp/` dir (gitignored, not committed).

**VERDICT: GO**
