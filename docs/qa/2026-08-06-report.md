# QA Prod Report — 2026-08-06

**VERDICT: NO-GO**

Core plan generation is DOWN in production. Fresh generation (goal #136) failed with an
upstream LLM error surfaced raw to the user: `Meal plan generation failed: 403 Your project
has been denied access. Please contact support.` — this reads as the Gemini API project being
denied access (distinct from the 2026-07-27 credit depletion). No retry recovered within
~6 minutes, the plan is permanently `failed`, and the user's free credit was consumed anyway.
The primary goal of this run — end-to-end verification of PR #54 (`nutritional_info`
tolerance + per-slot calorie defaults) — could NOT be exercised because generation never
reaches that code path.

Flow A (public surface) is fully healthy, including both data repairs from last night
(recipe 62 rebuild, slug resync + 301s). Checkout still reaches Stripe. But a diet planner
that cannot generate plans is not shippable.

## Results

| Flow | Check | Result | Notes |
|------|-------|--------|-------|
| A | Landing `/` hero + CTA | PASS | Hero renders; "Vytvořit jídelníček zdarma" routes to `/login`. |
| A | Consent banner | PASS | "Odmítnout" and "Přijmout" both dismiss; consent persisted in `mkt_consent_v1`. |
| A | `/recepty` listing | PASS | 24 cards, all with images, all linking to details. Note: recipe 59 (expected in resync set) is not in the listing — possibly unpublished; cosmetic, verify intent. |
| A | Slug resync (54/56/58) | PASS | Card slugs match titles: `56/menemen/`, `54/recka-cizrnova-polevka-revithosoupa/`, `58/gulasova-polevka-z-mleteho-masa/`. |
| A | Stale slug 301 | PASS | `/recepty/56/thajsky-okurkovy-salat/` → 301 → `/recepty/56/menemen/` (200). |
| A | Recipe 62 rebuild | PASS | 12-muffin batch: 170 g mandlová mouka at 12 porcí, coherent 6-step instructions, "Nutriční hodnoty" section present — 192 kcal / 4 g B / 12 g S / 8 g T per portion. |
| A | Recipe 56 detail | PASS | Content matches title (Menemen, turecké jídlo), ingredients + postup + nutrition present. |
| A | `/pricing` | PASS | Three tiers render: Zdarma 0 Kč / Standard 99 Kč / Premium 199 Kč + FAQ. |
| A | Legal `/privacy` | PASS | Real GDPR content (správce, shromažďované údaje, …). |
| A | Mobile 390×844 hamburger | PASS | "Otevřít menu" opens (→ "Zavřít menu", expanded), links visible, Recepty link navigates. |
| A | Console errors (all public pages) | PASS | Zero console errors on every page visited. |
| B | Login (QA account) | PASS | Username/password login lands on authed "Vaše plány" dashboard; "1 plánů zdarma zbývá" pre-run. |
| B | Old plan #134 repair | PASS (with note) | No micro-kcal meals anywhere (min 192 kcal). All 9 slots filled, lunches/dinners plated mains. Day totals computed from per-meal kcal: 1076 / 1810 / 1781 (avg 1556) — these do NOT match the expected ~1800/2650/2681; day 1 (1076) is below the 1200 floor. Repair achieved its hard goal (no 16-kcal meals) but the expected totals were not observed — worth a look. |
| B | Fresh plan generation (PR #54) | **FAIL** | Goal #136 (3 days, "Zdravý vyvážený jídelníček pro 2 osoby…", Praha). Failed in <60 s with raw upstream error shown to user: `Meal plan generation failed: 403 Your project has been denied access. Please contact support.` Re-checked at ~3 min and ~6 min after reload — still `failed`, no silent Celery retry recovery. This is a TOTAL generation outage (LLM access denied), not the previous AttributeError mode. PR #54 fix untestable. |
| B | Free credit accounting | **FAIL** | Credit went 1 → 0 despite the generation failing. User pays a credit for a backend outage. |
| B | Per-meal calorie plausibility (fresh plan) | SKIP | No plan produced — generation failed upstream. |
| B | Checkout reaches Stripe | PASS | "Vybrat Standard" on `/pricing` → `checkout.stripe.com` loaded (live `cs_live_…` session). Stopped immediately, nothing entered. |
| B | Logout | PASS | "Odhlásit se" returns to `/login`. |

## Console errors

None. Zero console errors across all pages in both flows (Stripe Checkout logged 2 benign warnings only).

## Failure evidence (local, gitignored — not committed)

- `/opt/llmDietPlanner/.claude/worktrees/fix-recipe-slug-update-fields/.playwright-mcp/plan-136-generation-failed.png` — raw 403 error screen shown to user on `/plan/136`.
- `/opt/llmDietPlanner/.claude/worktrees/fix-recipe-slug-update-fields/.playwright-mcp/plans-list-136-failed-credit-consumed.png` — plans list showing `#136 failed` and "0 plánů zdarma zbývá".

## Action items

1. **P0 — restore Gemini API access.** `403 Your project has been denied access` on project used by prod; contact Google support / check billing & abuse status for project 1058421808506. No user can generate a plan right now. (Related: still no 429/403 alerting.)
2. **P1 — stop consuming free credits on failed generations** (refund/void credit when a plan ends in `failed`).
3. **P1 — stop surfacing raw upstream error strings** ("403 Your project has been denied access…") to end users.
4. **P2 — re-run this QA once access is restored** to actually exercise PR #54; the QA account will need its free credit reset (the one allotted generation was consumed by the failed #136).
5. **P3 — check plan #134 repair math**: observed day totals 1076/1810/1781 vs expected ~1800/2650/2681; day 1 sits under 1200 kcal. Also confirm whether public recipe 59 is intentionally unpublished.
