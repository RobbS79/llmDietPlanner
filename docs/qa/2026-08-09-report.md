# QA Prod Report — 2026-08-09

**VERDICT: NO-GO**

Post-deploy verification of eatalnicek.eu after PR #59 (chef-chat invite card, auto-open chat, persistent swap banner, resumable web research). All shipped *mechanics* work, including the headline reload-resume fix. NO-GO is driven by two content-level failures a real user notices immediately: a masculine self-reference in the new web-research completion message (breaks the "naše kuchařka" persona, explicit FAIL condition for this run) and meat-only swap candidates served inside a vegetarian plan.

Authed as seeded QA account (user 24) via token injection; login form not exercised (by design of this run).

## Results

| Flow | Check | Result | Notes |
|---|---|---|---|
| A | Invite card on recipe page | PASS | Heading "Nesedí vám tohle jídlo?", body "Napište naší kuchařce, co byste chtěli jinak…", all 4 intent chips present, green button "Poradit se s kuchařkou" (rgb(63,133,87), white text). |
| A | Old "Vyměnit recept" removed | PASS | String absent from entire page body (checked programmatically). |
| B | Intent chip auto-sends | PASS | "Chci něco rychlejšího" opened chat, appeared immediately as user bubble, input disabled while waiting, real assistant reply ("Vybrala jsem pro vás balkánské čevabčiči…") in ~15 s. |
| C | "Nesedí?" on plan meal cards | PASS | All 9 meal cards on /plan/138 have the button. Real click on Den 1 Oběd navigated in ONE click to `/plan/138/recipe/138:1:lunch:0?chat=1` with chat already open. Caveat: every meal in plan 138 has a food image, so the no-image layout variant could not be exercised; Playwright's actionability check (fails on intercepted pointer) passed on the card tested. |
| D | Candidate headings + per-card accept | PASS (both variants seen) | Multi-candidate: "Vyberte, co vám sedí nejvíc" over 3 cards, each with its own "Použít tento recept". Single candidate (web result): "Návrh pro vás". "Ukázat jiné návrhy" present in both. |
| D* | Candidate content quality | FAIL | Plan 138 is explicitly vegetarian ("vegetariánská strava" in the plan prompt), yet all 3 candidates for "Chci něco rychlejšího" were meat dishes: Čevabčiči, Kuřecí perkelt I., Frankfurtská polévka. Also not faster: 40/65/40 min vs current 22 min, and 1049–1304 kcal vs 538. Screenshot: `/opt/llmDietPlanner/.playwright-mcp/D-candidates-meat-in-veg-plan.png` |
| E | Persistent swap banner + undo | PASS | Accepted Čevabčiči swap → green banner (bg rgb(231,240,232)) "Hotovo — místo „Špagety Aglio, Olio e Peperoncino“ máte teď „Čevabčiči“." with "Vrátit původní recept". Still present after 12 s (not a toast). Undo restored the original recipe and removed the banner. |
| F | Web research waiting UX | PASS | Real staged states observed: "Chystám hledání…" → "Hledám recepty na webu…" → "Čtu nalezený recept a přepočítávám porce…". Elapsed timer ticks (0:07 → 0:28 → 0:40 continuing across reload), three step dots (zadáno / hledám / čtu recept), "Zrušit hledání" button, reassurance copy present. |
| F | Reload mid-search resumes (headline fix) | PASS | Reloaded at ~0:30 while searching. Chat panel reopened BY ITSELF with "Pokračuju v hledání receptu na webu — počkejte chviličku.", timer continued (0:40, not reset), and the result was delivered after ~70 s total: "Domácí japonský ramen s bůčkem (podle Kitchenette)" as a usable candidate card. |
| G | Female-cook persona | FAIL | Header "Chat s naší kuchařkou" correct; corpus-path replies feminine ("Vybrala jsem…", "…podívala jsem se"). BUT the web-research completion message is masculine: "**Našel jsem** na webu recept: …" — should be "Našla jsem". This is the new PR #59 code path. Other Czech copy reads naturally. |
| H | Landing (logged out) | PASS | Marketing hero, CTA, sections, footer render; no console errors. Consent banner not re-shown (consent persisted in this browser profile from prior runs — not re-testable without clearing state). |
| H | /recepty | PASS | Full recipe cards with images, descriptions, deals badges ("N ve slevě"), real links to detail pages; no console errors. |
| H | Public recipe detail (/recepty/68/chana-masala/) | PASS | Title, deals headline ("3 z 16 surovin ve slevě tento týden"), Ingredience, Postup, per-portion nutrition all render; proper page title "Chana Masala — Recept \| Vařto"; no console errors. |
| H | /pricing | PASS | Three tiers render: Zdarma 0 Kč / Standard 99 Kč/měsíc / Premium 199 Kč/měsíc, FAQ, CTA; no console errors. |
| H | Plan 138 renders | PASS | All 3 days × 3 meals with names, images, macros, kcal, prep times; totals bar (1626 kcal/day avg) present. |

\* D mechanics passed; the dietary-preference violation is recorded as its own FAIL row because it is the thing a real user would call broken.

## Console errors

One across the entire session, benign:

- `https://eatalnicek.eu/api/auth/profile/` → 401, once, on first load with a stale access token before silent refresh (session recovered immediately). No errors on any other page (plan, recipe, chat, landing, /recepty, recipe detail, /pricing).

## Screenshots (local, not committed — authed content)

- `/opt/llmDietPlanner/.playwright-mcp/A-invite-card.png` — invite card + chips + green button
- `/opt/llmDietPlanner/.playwright-mcp/D-candidates-meat-in-veg-plan.png` — FAIL evidence: meat candidates in vegetarian plan
- `/opt/llmDietPlanner/.playwright-mcp/E-swap-banner.png` — persistent green swap banner after 12 s
- `/opt/llmDietPlanner/.playwright-mcp/F-searching-stage2.png` — search stage with timer/dots/cancel
- `/opt/llmDietPlanner/.playwright-mcp/F-after-reload-resumed.png` — auto-reopened chat after mid-search reload
- `/opt/llmDietPlanner/.playwright-mcp/F-web-result-ramen.png` — delivered web result card

## State left behind (QA account, plan 138)

- Meal 138:1:lunch:0 was swapped to Čevabčiči and then reverted via "Vrátit původní recept" — net zero; plan shows the original Špagety Aglio, Olio e Peperoncino again.
- Chat history persists on meal 138:1:lunch:0 (two conversations: "Chci něco rychlejšího" and the ramen web search).
- Web research produced a draft/derived recipe "Domácí japonský ramen s bůčkem (podle Kitchenette)"; it was NOT applied to the plan, but any backing recipe row created by the search likely persists.
- No plan generated, no checkout attempted, no other data touched.

## Recommended follow-ups

1. **(Blocker for GO) Fix masculine "Našel jsem" in the web-research completion message** — should be "Našla jsem na webu recept: …". Single-string fix in the new PR #59 result-message path; everything else in the persona is consistently feminine.
2. **(Blocker-adjacent) Swap candidates ignore the plan's dietary constraint** — vegetarian plan got 3/3 meat candidates for a generic "faster" ask. Matches the known Grounding Selection Quality Gap (meat facets, EN/CZ). The chat swap path needs the same hard dietary filter the generator has.
3. Candidate relevance to the stated intent: "Chci něco rychlejšího" returned 40–65 min recipes against a 22 min original (and a 30 min plan constraint). Add prep-time (and kcal-proximity) constraints to candidate ranking.
4. Web-result nutrition basis: the ramen card shows "4100 kcal" despite the "přepočítávám porce" stage — looks like whole-recipe values surfaced as the card metric (same family as the known corpus per-portion basis issue). Audit the web-import portion recompute.
5. Minor: web result offered a 300-min recipe into a "Max 30 minut" plan — arguably honest for an explicit "pravý ramen" ask, but consider a gentle note in the reply ("pozor, tenhle je na dlouho").
6. Not verifiable this run: the "Nesedí?" clickability fix on meals WITHOUT food images (plan 138 has images on all 9 meals). If an image-less plan exists, spot-check once.
